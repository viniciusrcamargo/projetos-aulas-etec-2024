const alunos = ['Davi', 'Henrique', 'Everton', 'Matheus'];

let i = 0;//variável contadora //while significa enquanto
while(i < alunos.length){//length tamanho total do array
    console.log("Aluno da Etec - " + alunos[i]);
    i++;//incrementa mais 1
}